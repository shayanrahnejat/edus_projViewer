export function IrancellAdminReportsPage(){const{state}=useIrancellStore(),r=state.admin.reports,items=[['کاربر فعال ماهانه',IrancellFormatPersianNumber(r.mau),Users],['GMV',IrancellFormatCurrency(r.gmv),WalletCards],['نرخ حل چیستی',`${IrancellFormatPersianNumber(r.chistiResolution)}٪`,BrainCircuit],['تبدیل به پرداخت',`${IrancellFormatPersianNumber(r.paidConversion)}٪`,TrendingUp],['تکمیل رضایت',`${IrancellFormatPersianNumber(r.consentCompletion)}٪`,ShieldCheck],['موفقیت کلاس',`${IrancellFormatPersianNumber(r.classSuccess)}٪`,Activity]];return <><IrancellPageHeader eyebrow="تحلیل محصول" title="داشبورد شاخص‌های کلیدی" description="رویدادهای استاندارد قیف، اعتماد، پرداخت، کلاس و کیفیت تجمیع شده‌اند."/><div className="ir-stats-grid">{items.map(([label,value,icon])=><IrancellStatCard key={label} label={label} value={value} icon={icon}/>)}</div></>}