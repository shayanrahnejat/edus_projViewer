export function IrancellSharedConsentDetailsPage({params,onNavigate}){const{state,dispatch}=useIrancellStore(),[name,setName]=useState(''),d=state.consent.documentsById[params.id];if(!d)return <IrancellStatePanel state="error" title="سند رضایت پیدا نشد" description="شناسه سند معتبر نیست."/>;return <><IrancellPageHeader eyebrow="امضایی" title="رضایت‌نامه کلاس زنده" description="امضای والد به همراه رابطه هویتی، زمان و شناسه ممیزی ثبت می‌شود."/><IrancellCard action={<IrancellStatusBadge status={d.status}/>}><div className="ir-legal-document"><h2>متن رضایت‌نامه</h2><p>{d.documentText}</p><ul><li>مدرس و زمان جلسه پیش از امضا مشخص شده‌اند.</li><li>ورود فقط برای شرکت‌کنندگان مجاز فعال است.</li><li>عملیات حساس دارای رد ممیزی است.</li></ul></div>{d.status!=='signed'?<><IrancellInput label="نام کامل والد" value={name} onChange={e=>setName(e.target.value)}/><IrancellButton onClick={()=>dispatch(IrancellConsentSign(d.id,name||'مریم رضایی'))}>امضا و تأیید</IrancellButton></>:<IrancellButton onClick={()=>onNavigate(`payment/${state.classroom.sessionsById[d.sessionId]?.orderId}`)}>ادامه به پرداخت</IrancellButton>}</IrancellCard></>}