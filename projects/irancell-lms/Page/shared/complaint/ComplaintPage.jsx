export function IrancellSharedComplaintPage({params,onNavigate}){const{dispatch}=useIrancellStore(),[category,setCategory]=useState('عدم حضور مدرس'),[description,setDescription]=useState(''),sessionId=params.id==='new'?'class-demo-1':params.id;function submit(){if(!description.trim())return;dispatch(IrancellQualitySubmitComplaint(sessionId,category,description));onNavigate('parent/complaints')}return <><IrancellPageHeader eyebrow="رسیدگی" title="ثبت شکایت" description="موضوع، شرح و شواهد را دقیق ثبت کنید. تصمیم و اقدام مالی دارای رد ممیزی خواهد بود."/><IrancellCard><IrancellSelect label="دسته شکایت" value={category} onChange={e=>setCategory(e.target.value)} options={['عدم حضور مدرس','کیفیت نامناسب','مشکل فنی','رفتار نامناسب','درخواست بازگشت وجه'].map(x=>({value:x,label:x}))}/><IrancellTextarea label="شرح کامل" rows={7} value={description} onChange={e=>setDescription(e.target.value)} helper="زمان رخداد و اثر آن بر جلسه را بنویسید."/><IrancellButton onClick={submit} disabled={!description.trim()}>ثبت و دریافت کد پیگیری</IrancellButton></IrancellCard></>}