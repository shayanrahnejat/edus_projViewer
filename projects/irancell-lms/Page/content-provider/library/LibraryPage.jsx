export function IrancellContentProviderLibraryPage(){const{state}=useIrancellStore(),items=Object.values(state.content.catalogueById);return <><IrancellPageHeader eyebrow="کتابخانه محتوا" title="محتواهای من" description="وضعیت انتشار، کیفیت، امتیاز و بازدید را پیگیری کنید."/><div className="ir-card-list">{items.map(c=><IrancellCard key={c.id} title={c.title} subtitle={`${c.grade} • ${c.subject} • ${c.topic}`} action={<IrancellStatusBadge status={c.status}/>}><div className="ir-content-metrics"><span>امتیاز {c.rating||0}</span><span>{Number(c.views||0).toLocaleString('fa-IR')} بازدید</span><span>{c.provider}</span></div></IrancellCard>)}</div></>}