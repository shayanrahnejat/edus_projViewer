export function IrancellContentCard({content,progress=0,onOpen}){return <article className="ir-content-card" onClick={()=>onOpen?.(content)} tabIndex={0} onKeyDown={e=>{if(e.key==='Enter')onOpen?.(content)}}><div className="ir-content-card__cover"><PlayCircle size={34}/><span>{content.duration||'۱۲ دقیقه'}</span></div><div className="ir-content-card__body"><small>{content.grade} • {content.subject}</small><h3>{content.title}</h3><p>{content.description}</p><div className="ir-content-card__meta"><span><Star size={14}/>{IrancellFormatPersianNumber(content.rating||0)}</span><span><Clock3 size={14}/>{content.duration||'۱۲ دقیقه'}</span><span><BookOpen size={14}/>{content.level||'متوسط'}</span></div>{progress>0&&<div className="ir-progress"><span style={{width:`${progress}%`}}/><small>{IrancellFormatPersianNumber(progress)}٪</small></div>}</div></article>}