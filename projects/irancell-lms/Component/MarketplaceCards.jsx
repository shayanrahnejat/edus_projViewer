export function IrancellProviderCard({provider,onView,onSelect,selectLabel='انتخاب مدرس'}){return <article className="ir-provider-card"><div className="ir-provider-card__avatar">{provider.type==='academy'?<Building2/>:<UserRoundCheck/>}</div><div className="ir-provider-card__main"><div className="ir-provider-card__title"><h3>{provider.name}</h3>{provider.verificationStatus==='verified'&&<span><CheckCircle2 size={15}/>تأییدشده</span>}</div><p>{provider.bio||'مدرس متخصص و همراه یادگیری'}</p><div className="ir-provider-card__meta"><span><Star size={15}/>{IrancellFormatPersianNumber(provider.rating||0)}</span><span>{IrancellFormatPersianNumber(provider.completedSessions||0)} جلسه</span><span>از {IrancellFormatCurrency(provider.basePrice||0)}</span></div></div><div className="ir-provider-card__actions"><button type="button" onClick={()=>onView?.(provider)}>مشاهده پروفایل</button>{onSelect&&<button type="button" className="primary" onClick={()=>onSelect(provider)}>{selectLabel}</button>}</div></article>}
export function IrancellOfferCard({offer,provider,onSelect,selected=false}){return <article className={`ir-offer-card ${selected?'is-selected':''}`}><header><div><h3>{provider?.name||offer.assignedTeacherName||'تأمین‌کننده آموزشی'}</h3><p>{offer.assignedTeacherName&&`مدرس جلسه: ${offer.assignedTeacherName}`}</p></div>{provider?.verificationStatus==='verified'&&<CheckCircle2 size={20}/>}</header><div className="ir-offer-card__details"><span><CalendarDays size={17}/>{new Date(offer.proposedTime).toLocaleString('fa-IR')}</span><span><Clock3 size={17}/>جلسه ۶۰ دقیقه‌ای</span><strong>{IrancellFormatCurrency(offer.price)}</strong></div>{onSelect&&<button type="button" onClick={()=>onSelect(offer)} disabled={selected}>{selected?'انتخاب شده':'انتخاب پیشنهاد'}</button>}</article>}